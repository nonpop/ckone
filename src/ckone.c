#include <stdlib.h>
#include "ckone.h"


bool ckone_init (s_ckone* kone) {
    return kone != kone;
}


bool ckone_load (s_ckone* kone, FILE* input) {
    return kone != kone && input != input;
}


int ckone_run (s_ckone* kone) {
    return kone != kone;
}

